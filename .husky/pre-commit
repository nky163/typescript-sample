#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh" 2>/dev/null || true

# Run ESLint --fix on staged TS/JS via lint-staged (package.json config)
npm run lint:fix --if-present || exit 1
npx lint-staged --no-stash || exit 1
