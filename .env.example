# Application
PORT=3000
TRANSFER_THRESHOLD=1000000
NODE_ENV=development

# Database connection (individual fields preferred)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=buckpal

# Optional TypeORM flags for local dev
TYPEORM_LOGGING=true
TYPEORM_SYNCHRONIZE=false